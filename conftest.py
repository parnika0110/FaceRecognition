"""Pytest root configuration.

An empty conftest.py at the repository root makes `pytest` (and
`python -m pytest`) insert this directory into sys.path, so the `faceid`
package imports correctly on a fresh clone with either invocation.
"""
